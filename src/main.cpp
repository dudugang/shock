#include <iostream>
#include <flowfield.h>
using std::cout;
using std::endl;

int main(int argc, char* argv[]) {
    // Get inputs
    Inputs inputs;

    // Initialize flowfield
    Flowfield flow(inputs);

    return 0;
}
