#include <ghost.h>
#include <vector>
using std::vector;

/*
// Constructor
Ghost::Ghost(double x, double u, int cell_id, vector<int> neighbors)
    : Cell(x, u, cell_id, neighbors) {
    this->type = "flow";
}
*/
